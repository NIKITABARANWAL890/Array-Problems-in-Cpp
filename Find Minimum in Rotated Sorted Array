class Solution {
public:
    int findMin(vector<int>& nums) {
        int n=nums.size();
        int min_ele = nums[0];
        for(int i=0; i<n; i++){
            int temp = nums[i];
            min_ele = min(temp, min_ele);
        }
        return min_ele;
    }
};
